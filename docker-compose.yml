# This is an example configuration for Docker Compose. Make sure to atleast update
# the cookie secret & postgres database password.
#
# Some other recommendations:
# 1. To persist Postgres data, assign it a volume host location.
# 2. Split the worker service to adhoc workers and scheduled queries workers.
version: '2'
services:
  server:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile.web
    command: server
    depends_on:
      - postgres
      - redis
    environment:
      PORT: 5000
      PYTHONUNBUFFERED: 0
      REDASH_LOG_LEVEL: "INFO"
      REDASH_REDIS_URL: "redis://redis:6379/0"
      REDASH_DATABASE_URL: "postgresql://postgres@postgres/postgres"
      REDASH_COOKIE_SECRET: veryverysecrettoken
      REDASH_SECRET_KEY: veryverysecretkey
      REDASH_WEB_WORKERS: 4
      SNOWFLAKE_PRIVATE_KEY_B64: "${SNOWFLAKE_PRIVATE_KEY_B64}"
      REDASH_ADDITIONAL_QUERY_RUNNERS: "${REDASH_ADDITIONAL_QUERY_RUNNERS}"
  scheduler:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile.scheduler
    command: scheduler
    environment:
      PYTHONUNBUFFERED: 0
      REDASH_LOG_LEVEL: "INFO"
      REDASH_REDIS_URL: "redis://redis:6379/0"
      REDASH_DATABASE_URL: "postgresql://postgres@postgres/postgres"
      REDASH_COOKIE_SECRET: veryverysecrettoken
      REDASH_SECRET_KEY: veryverysecretkey
      SNOWFLAKE_PRIVATE_KEY_B64: "${SNOWFLAKE_PRIVATE_KEY_B64}"
      REDASH_ADDITIONAL_QUERY_RUNNERS: "${REDASH_ADDITIONAL_QUERY_RUNNERS}"
  worker:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile.worker
    command: worker
    environment:
      PYTHONUNBUFFERED: 0
      REDASH_LOG_LEVEL: "INFO"
      REDASH_REDIS_URL: "redis://redis:6379/0"
      REDASH_DATABASE_URL: "postgresql://postgres@postgres/postgres"
      QUEUES: "queries scheduled_queries schemas periodic emails default"
      WORKERS_COUNT: 4
      REDASH_COOKIE_SECRET: veryverysecrettoken
      REDASH_SECRET_KEY: veryverysecretkey
      SNOWFLAKE_PRIVATE_KEY_B64: "${SNOWFLAKE_PRIVATE_KEY_B64}"
      REDASH_ADDITIONAL_QUERY_RUNNERS: "${REDASH_ADDITIONAL_QUERY_RUNNERS}"
  redis:
    platform: linux/amd64
    image: redis:3.0-alpine
  postgres:
    platform: linux/amd64
    image: postgres:9.5.6-alpine
    # volumes:
    #   - /opt/postgres-data:/var/lib/postgresql/data
  nginx:
    platform: linux/amd64
    image: redash/nginx:latest
    ports:
      - "8080:80"
    depends_on:
      - server
    links:
      - server:redash
